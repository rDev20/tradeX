# @tradex/marketing

The public-facing marketing site. Static generation via Next.js, hosted on Cloudflare Pages.

Routes: `/`, `/pricing`, `/how-it-works`, `/leaderboard`, `/blog/*`, `/legal/*`.

Designed to outperform the app on SEO — LCP target < 1.5s, pre-rendered, minimal JS.
