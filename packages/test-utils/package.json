{
  "name": "@tradex/test-utils",
  "version": "0.0.1",
  "private": true,
  "description": "Shared test helpers — fixtures, factories, mocks",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint 'src/**/*.ts' --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tradex/types": "workspace:*"
  },
  "devDependencies": {
    "@tradex/eslint-config": "workspace:*",
    "@tradex/tsconfig": "workspace:*"
  }
}
