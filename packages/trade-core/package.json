{
  "name": "@tradex/trade-core",
  "version": "0.0.1",
  "private": true,
  "description": "Core trade-lifecycle math — PnL, trailing SL, partial exits, state transitions",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint 'src/**/*.ts' --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@tradex/types": "workspace:*"
  },
  "devDependencies": {
    "@tradex/eslint-config": "workspace:*",
    "@tradex/tsconfig": "workspace:*"
  }
}
